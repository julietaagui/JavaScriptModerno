let elemento;

// Acceder al DOM con JavaScript
elemento = document;

// Acceder a todos los elementos del DOM
elemento = document.all;

// Acceder a la sección <head> del documento
elemento = document.head;

// Acceder a la sección <body> del documento
elemento = document.body;

// Obtener el dominio del documento
elemento = document.domain;

// Acceder a todos los formularios del documento
elemento = document.forms;

// Acceder al primer formulario del documento
elemento = document.forms[0];

// Obtener el ID del primer formulario
elemento = document.forms[0].id;

// Obtener el método de envío del primer formulario
elemento = document.forms[0].method;

// Obtener las clases CSS del primer formulario
elemento = document.forms[0].classList;

// Obtener o establecer la URL de envío del primer formulario
elemento = document.forms[0].action;


// Acceder a todos los enlaces del documento
elemento = document.links;

// Acceder al quinto enlace del documento
elemento = document.links[4];

// Obtener las clases CSS del quinto enlace del documento
elemento = document.links[4].classList;

// Obtener el atributo class del quinto enlace del documento
elemento = document.links[4].className;

// Acceder a todas las imágenes del documento
elemento = document.images;

// Acceder a todos los scripts del documento
elemento = document.scripts;

console.log(elemento)