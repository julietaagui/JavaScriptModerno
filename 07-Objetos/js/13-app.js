const producto = {
    nombre: "Monitor",
    precio: 300,
    disponible: true
};

// Retorna las llaves del arreglo
console.log( Object.keys(producto));

// Retorna valor
console.log(Object.values(producto));

// Retorna todo
console.log(Object.entries(producto));

